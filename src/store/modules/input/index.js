import state from './state'
import { add as addInput, cancel as cancelInput } from './actions'

export default {
  state,
  mutations: {},
  getters: {},
  actions: {
    addInput,
    cancelInput
  }
}
